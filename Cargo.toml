[package]
name = "nvse-lsp"
version = "0.1.0"
edition = "2021"

[profile.release]
lto = "fat"
panic = "abort"
opt-level = 3

[dependencies]
dashmap = "5.5"
tokio = { version = "1.37", features = ["full"] }
tower-lsp = "0.20"

[dev-dependencies]
similar-asserts = "1.5"
